import { styled } from '@mui/material/styles';

export const ImageStyle = styled('img')(() => ({
  width: '200px',
  height: '200px',
  objectFit: 'contain',
  marginTop: 5,
  borderRadius: '5px',
}));
